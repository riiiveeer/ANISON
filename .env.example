# 本地开发默认不启用 Beta 门禁。公网 Beta 部署时必须同时配置下面两项。
BETA_AUTH_USERNAME=
BETA_AUTH_PASSWORD=

# report-only（默认）只报告 CSP 违规；完成公网冒烟后可切换为 enforce。
CSP_MODE=report-only

# 部署平台通常会自动注入 PORT；本地默认使用 3000。
PORT=3000

# 不要在服务端配置 DeepSeek Key。ANISON 只转发每位用户自己提供的 Bearer Key。
